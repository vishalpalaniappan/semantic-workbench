## Overview

This repo contains a nodejs server which acts as the backend for the design workbench. 

The workspace folder contains the design files that are loaded by the workbench. Currently, there is support for loading on designs workspace but this will be extended in the future for multiple workspaces.

### Usage

## Install Libraries

```
npm run install
```

### Run Application

```
npm run dev
```